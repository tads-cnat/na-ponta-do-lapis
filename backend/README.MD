# Como rodar a aplicação Backend Spring Boot 

1. Entrar na pasta do backend

```bash
cd backend/na-ponta-do-lapis
```

2. Limpar e compilar o projeto

```bash
./mvnw clean install
```
3. Rodar a aplicação

```bash
./mvnw spring-boot:run
```